# Математическое описание Obsidian Vault V3

Этот документ задаёт математическую модель реализованного в проекте алгоритма
Obsidian Vault V3. Нормативной реализацией остаётся исходный код; цель документа
— выписать конструкцию в формулах, показать, какие свойства следуют строго из
её устройства, и не смешивать их с неподтверждёнными криптографическими
предположениями.

## 1. Обозначения и пространства

- \(\mathbb W=\mathbb Z/2^{64}\mathbb Z\) — множество 64-битных слов.
- \(x+y\) и \(xy\) для \(x,y\in\mathbb W\) означают сложение и умножение
  по модулю \(2^{64}\).
- \(x\oplus y\) — побитовое XOR.
- \(\neg x\) — побитовое дополнение 64-битного слова.
- \(\operatorname{rotl}(x,t)\) и \(\operatorname{rotr}(x,t)\) — циклические
  сдвиги 64-битного слова на \(t\bmod 64\) позиций.
- \(\operatorname{LE}_w(x)\) — little-endian представление целого в \(w\)
  битах.
- \(X\mathbin\|Y\) — конкатенация строк байтов.
- \(|X|\) — длина строки \(X\) в байтах.
- \(0^n\) — строка из \(n\) нулевых битов или байтов, в зависимости от
  контекста.
- \(\operatorname{Trunc}_n(X)\) — первые \(n\) битов строки \(X\).

Состояние перестановки — вектор

\[
S=(s_0,\ldots,s_{15})\in\mathbb W^{16},
\]

то есть 1024 бита. При преобразовании байтов в слова и обратно всегда
используется little-endian порядок.

## 2. Перестановка P1024-V3

### 2.1. Сеть Фейстеля

Состояние делится на две 512-битные половины:

\[
L^{(r)}=(s_0,\ldots,s_7),\qquad
R^{(r)}=(s_8,\ldots,s_{15}).
\]

Для \(r=0,\ldots,47\) выполняется balanced Feistel-раунд

\[
\begin{aligned}
L^{(r+1)} &= R^{(r)},\\
R^{(r+1)} &= L^{(r)}\oplus F_r(R^{(r)}).
\end{aligned}
\]

XOR в последней формуле применяется покомпонентно к восьми словам. Полная
перестановка имеет вид

\[
P=\Phi_{47}\circ\Phi_{46}\circ\cdots\circ\Phi_0.
\]

Раунд обратим независимо от того, обратима ли \(F_r\). Из выходных половин
получаем

\[
\begin{aligned}
R^{(r)} &= L^{(r+1)},\\
L^{(r)} &= R^{(r+1)}\oplus F_r(L^{(r+1)}).
\end{aligned}
\]

Следовательно, каждый \(\Phi_r\) и композиция \(P\) являются биекциями на
\(\{0,1\}^{1024}\). Это строгое структурное свойство, а не статистический
вывод.

### 2.2. Раундовая функция

Раундовая функция состоит из четырёх последовательных ступеней:

\[
F_r=D_3\circ N_{r,3}\circ D_2\circ N_{r,2}
       \circ D_1\circ N_{r,1}\circ D_0\circ N_{r,0},
\]

где фактический порядок применения к вектору \(x\) таков:

\[
x^{(0)}=x,\qquad
x^{(j+1)}=D_j(N_{r,j}(x^{(j)})),\quad j=0,1,2,3,
\]

и \(F_r(x)=x^{(4)}\).

### 2.3. Позиционные константы

Пусть

\[
\begin{aligned}
\sigma &= \mathtt{4f4253494449414e},\\
a &= \mathtt{9e3779b97f4a7c15},\\
b &= \mathtt{d1b54a32d192ed03},\\
c &= \mathtt{94d049bb133111eb},\\
d &= \mathtt{d6e8feb86659fd93},\\
e &= \mathtt{a5a3564e27f8864d}.
\end{aligned}
\]

Для номера раунда \(r\), ступени \(j\) и позиции \(i\) сначала вычисляется

\[
v_0=\sigma\oplus (ra)\oplus(jb)\oplus(ic),
\]

где произведения берутся в \(\mathbb W\). Затем

\[
\begin{aligned}
v_1 &= v_0\oplus\operatorname{rotl}(v_0,17)
             \oplus\operatorname{rotr}(v_0,11),\\
v_2 &= d v_1,\\
v_3 &= v_2\oplus(v_2\mathbin{\gg}29),\\
v_4 &= e v_3,\\
t &= ((r+13j+7i)\bmod63)+1,\\
\operatorname{RC}(r,j,i)&=v_4\oplus\operatorname{rotl}(v_4,t).
\end{aligned}
\]

Все операции над \(v_k\), кроме явно указанного логического сдвига, выполняются
в \(\mathbb W\). Константы публичны и не являются частью ключа. Используемое
расписание содержит \(48\cdot4\cdot8=1536\) констант.

Начальное состояние duplex-конструкции задаётся ещё 16 значениями

\[
S_{\mathrm{IV},i}=\operatorname{RC}(48,4,i),\qquad 0\le i<16.
\]

### 2.4. Нелинейный слой

Множители по позициям:

\[
\begin{array}{c|c}
i&m_i\\\hline
0&\mathtt{9e3779b185ebca87}\\
1&\mathtt{c2b2ae3d27d4eb4f}\\
2&\mathtt{165667b19e3779f9}\\
3&\mathtt{85ebca77c2b2ae63}\\
4&\mathtt{27d4eb2f165667c5}\\
5&\mathtt{94d049bb133111eb}\\
6&\mathtt{d6e8feb86659fd93}\\
7&\mathtt{a5a3564e27f8864d}
\end{array}
\]

Таблица вращений \(\alpha_{j,i}\):

\[
\begin{array}{c|rrrrrrrr}
j\backslash i&0&1&2&3&4&5&6&7\\\hline
0&7&19&31&43&53&11&37&59\\
1&13&29&47&61&17&41&23&51\\
2&5&27&39&57&15&33&49&21\\
3&11&35&55&25&45&9&63&17
\end{array}
\]

Индексы слов далее берутся по модулю 8. Для входа
\(x=(x_0,\ldots,x_7)\) слой \(N_{r,j}\) возвращает \(y\), где

\[
\boxed{
y_i=\left(
x_i+operatorname{rotl}(x_{i+1},\alpha_{j,i})
+\operatorname{RC}(r,j,i)
\right)m_{(i+j)\bmod8}
\oplus
\operatorname{rotl}(x_{i+4},\alpha_{j,(i+3)\bmod8})
}
\]

для \(i=0,\ldots,7\).

Сложение и умножение по модулю \(2^{64}\) создают переносы между битами и
поэтому нелинейны над \(\mathbb F_2\). Все \(m_i\) нечётны, следовательно,
каждое отдельное отображение \(z\mapsto m_i z\pmod{2^{64}}\) обратимо. Это не
означает автоматически, что весь совместный слой \(N_{r,j}\) является
биекцией; для обратимости Feistel-перестановки такое требование не нужно.

### 2.5. Линейная диффузия

Для каждой ступени задана четвёрка вращений

\[
\begin{array}{c|rrrr}
j&\beta_{j,0}&\beta_{j,1}&\beta_{j,2}&\beta_{j,3}\\\hline
0&23&30&29&31\\
1&5&30&25&4\\
2&61&52&9&29\\
3&53&48&47&50
\end{array}
\]

Сначала вычисляется линейное над \(\mathbb F_2\) отображение

\[
z_i=y_i
\oplus\operatorname{rotl}(y_{i+1},\beta_{j,0})
\oplus\operatorname{rotl}(y_{i+3},\beta_{j,1})
\oplus\operatorname{rotl}(y_{i+5},\beta_{j,2})
\oplus\operatorname{rotl}(y_{i+6},\beta_{j,3}).
\]

После него выполняется перестановка восьми слов

\[
(D_j(y))_d=z_{\pi_j(d)},
\]

где

\[
\begin{array}{c|rrrrrrrr}
j\backslash d&0&1&2&3&4&5&6&7\\\hline
0&2&5&1&7&3&0&6&4\\
1&6&2&7&1&4&3&0&5\\
2&3&7&4&0&6&2&5&1\\
3&5&0&3&6&1&7&4&2
\end{array}
\]

Каждый \(D_j\) представим бинарной матрицей \(M_j\in\mathbb F_2^{512\times512}\).
Реализация строит её столбцы, подавая 512 базисных векторов, и выполняет
гауссово исключение над \(\mathbb F_2\). Для всех четырёх слоёв проверено

\[
\operatorname{rank}_{\mathbb F_2}(M_j)=512.
\]

Поэтому каждый линейный слой обратим. Полный ранг, однако, не равен доказанной
оптимальной дифференциальной или линейной стойкости и сам по себе не задаёт
branch number.

### 2.6. Цена одной перестановки

Если не учитывать вычисление позиционных констант, копирование слов и
индексацию, одна функция \(F_r\) содержит 32 умножения, 64 модульных сложения,
192 вращения и 160 XOR 64-битных слов. Feistel-смешивание добавляет 8 XOR.
Таким образом, 48 раундов содержат 1536 умножений, 3072 сложения, 9216 вращений
и 8064 XOR. Асимптотически \(P\) имеет постоянную стоимость, поскольку размер
состояния и число раундов фиксированы.

### 2.7. Нормативный проверочный вектор

Для нулевого 1024-битного входа реализация задаёт следующий выход; слова
перечислены в порядке \(s_0,\ldots,s_{15}\):

\[
\begin{aligned}
P(0^{1024})=(&\mathtt{7fe2d357055f8320},
\mathtt{62c4ecb12135ecdd},\\
&\mathtt{d1ed959ac297d6df},
\mathtt{1001183a8906549d},\\
&\mathtt{8e544f219b76a64d},
\mathtt{a1113114fca5fb20},\\
&\mathtt{c0403da15da6eec8},
\mathtt{8b09dd051d667511},\\
&\mathtt{a5e03cbb0bdfa664},
\mathtt{f59c9d702b81a443},\\
&\mathtt{b885c14af7627c9d},
\mathtt{924bb1d2d950dfe2},\\
&\mathtt{ba4e0a4935d10f5f},
\mathtt{ca56ec95107478fb},\\
&\mathtt{3c5075a6c126f70e},
\mathtt{2c5636991614b2f8}).
\end{aligned}
\]

Этот вектор фиксирует порядок слов, число раундов и точную семантику всех
64-битных операций. Он проверяет совместимость реализаций, но не является
криптографическим тестом стойкости.

## 3. Duplex/sponge над P1024-V3

### 3.1. Rate и capacity

Состояние разделено на

\[
\text{rate}=(s_0,\ldots,s_7)\in\{0,1\}^{512},\qquad
\text{capacity}=(s_8,\ldots,s_{15})\in\{0,1\}^{512}.
\]

Новый экземпляр начинается с \(S_{\mathrm{IV}}\), а не с нулевого состояния.

### 3.2. Однозначное обрамление поля

Для домена \(d\in\mathbb W\) и байтовой строки \(X\) строится

\[
U=\operatorname{LE}_{64}(d)\mathbin\|
  \operatorname{LE}_{64}(|X|)\mathbin\|X\mathbin\|\mathtt{01}.
\]

Пусть

\[
b(X)=\left\lceil\frac{17+|X|}{64}\right\rceil.
\]

Строка \(U\) дополняется нулями до \(64b(X)\) байтов, после чего последний
байт XOR-ится с \(\mathtt{80}\). Поэтому всегда поглощается хотя бы один блок,
а граница поля и его длина заданы дважды: явным счётчиком и padding-маркером.
Обозначим 64-байтовые блоки результата через \(B_0,\ldots,B_{b(X)-1}\).

### 3.3. Поглощение блока

Для блока с нумерацией \(q=0,\ldots,b(X)-1\) его восемь little-endian слов
\(w_0,\ldots,w_7\) XOR-ятся в rate. Одновременно изменяется capacity:

\[
\begin{aligned}
s_i&\leftarrow s_i\oplus w_i, &&0\le i<8,\\
s_8&\leftarrow s_8\oplus d,\\
s_9&\leftarrow s_9\oplus(q+1),\\
s_{14}&\leftarrow s_{14}\oplus\operatorname{rotl}(|X|,17),\\
s_{15}&\leftarrow s_{15}\oplus\neg q
  \oplus\operatorname{rotl}(d,31)\oplus\mathtt{0000000000000180},\\
S&\leftarrow P(S).
\end{aligned}
\]

Все остальные capacity-слова на этом шаге не меняются напрямую. Включение
домена, длины и номера блока в capacity дополнительно разделяет transcripts,
но криптографическая сила этого решения всё равно зависит от свойств \(P\).

### 3.4. Извлечение

Первый выходной блок — little-endian сериализация текущего rate:

\[
O_0=\operatorname{LE}_{64}(s_0)\|\cdots\|\operatorname{LE}_{64}(s_7).
\]

Для следующих блоков сначала применяется перестановка:

\[
S\leftarrow P(S),\qquad O_k=\operatorname{rate}(S),\quad k\ge1.
\]

Выход есть нужный префикс \(O_0\|O_1\|\cdots\). Для \(n>0\) байтов squeeze
добавляет \(\lceil n/64\rceil-1\) вызовов \(P\); первый блок не требует нового
вызова, поскольку последнее поглощение уже завершилось перестановкой.

### 3.5. Домены

Используются значения

\[
\begin{array}{c|c}
\text{поле}&d\\\hline
\text{algorithm}&\mathtt{4f56330000000001}\\
\text{master key/subkey}&\mathtt{4f56330000000002}\\
\text{subkey label/purpose}&\mathtt{4f56330000000003}\\
\text{header}&\mathtt{4f56330000000004}\\
\text{commitment}&\mathtt{4f56330000000005}\\
\text{nonce}&\mathtt{4f56330000000006}\\
\text{context}&\mathtt{4f56330000000007}\\
\text{message}&\mathtt{4f56330000000008}\\
\text{tag}&\mathtt{4f56330000000009}\\
\text{length}&\mathtt{4f5633000000000a}\\
\text{final}&\mathtt{4f5633000000000b}
\end{array}
\]

Далее \(\mathcal A_d(S,X)\) означает описанное поглощение поля \((d,X)\), а
\(\operatorname{Sq}_n(S)\) — извлечение \(n\) байтов.

## 4. Вывод подключей

Master key

\[
K\xleftarrow{\$}\{0,1\}^{512}
\]

получается от ОС. Общая метка алгоритма:

```text
OBSIDIAN-P1024-V3-DUPLEX-SIV
```

Для произвольной метки \(\ell\) определим

\[
\begin{aligned}
S_0&=S_{\mathrm{IV}},\\
S_1&=\mathcal A_{d_{alg}}(S_0,\texttt{ALGORITHM\_LABEL}),\\
S_2&=\mathcal A_{d_{key}}(S_1,K),\\
S_3&=\mathcal A_{d_{label}}(S_2,\ell),\\
S_4&=\mathcal A_{d_{final}}(S_3,\texttt{"derive-subkey"}),\\
\operatorname{KDF}(K,\ell)&=\operatorname{Sq}_{64}(S_4).
\end{aligned}
\]

Три 512-битных подключа:

\[
\begin{aligned}
K_{mac}&=\operatorname{KDF}(K,\texttt{"OBSIDIAN-V3-MAC-SUBKEY"}),\\
K_{stream}&=\operatorname{KDF}(K,\texttt{"OBSIDIAN-V3-STREAM-SUBKEY"}),\\
K_{commit}&=\operatorname{KDF}(K,\texttt{"OBSIDIAN-V3-COMMITMENT-SUBKEY"}).
\end{aligned}
\]

Подключи функционально разделены разными transcripts. Называть их
статистически независимыми можно только в модели, где KDF ведёт себя как PRF;
для собственной перестановки это является предположением, а не доказанным
фактом.

Перед конкретной операцией с подключом \(K_x\) создаётся новый keyed transcript:

\[
S_x=\mathcal A_{d_{label}}(
      \mathcal A_{d_{key}}(
       \mathcal A_{d_{alg}}(S_{\mathrm{IV}},\texttt{ALGORITHM\_LABEL}),K_x),
      \ell_x).
\]

## 5. Внутреннее сообщение и сокрытие длины

Пусть plaintext \(M\) имеет длину \(p\) байтов. Его внутренний заголовок:

\[
\mathtt{03}\|\mathtt{00}\|\mathtt{0000}\|\operatorname{LE}_{32}(p).
\]

Длина внутреннего сообщения равна

\[
m=32\left(\left\lfloor\frac{8+p}{32}\right\rfloor+1\right).
\]

Пусть \(\rho=m-8-p\). Тогда

\[
1\le\rho\le32,
\]

а \(R\xleftarrow{\$}\{0,1\}^{8\rho}\) заполняется ОС-источником случайности.
Внутренняя строка

\[
I=\mathtt{03}\|\mathtt{00}\|\mathtt{0000}\|
  \operatorname{LE}_{32}(p)\|M\|R,qquad |I|=m.
\]

Из открытой длины ciphertext наблюдатель узнаёт \(m\). Для \(m=32k\) это
ограничивает \(p\) интервалом

\[
\max(0,32k-40)\le p\le32k-9.
\]

Первый класс содержит длины \(0,\ldots,23\), последующие — по 32 возможных
длины. Следовательно, padding скрывает точную длину, но раскрывает её
32-байтовый класс.

## 6. Commitment, tag и шифрующий поток

Пусть

\[
H=\mathtt{4f42535633000100}
\]

— восьмибайтовый заголовок `OBSV3\0\x01\0`, \(A\) — обязательный непустой
context длиной не более 4096 байтов, а

\[
N\xleftarrow{\$}\{0,1\}^{192}
\]

— nonce.

Ниже запись \(\operatorname{Duplex}_{K_x,\ell_x}[\cdots;n]\) означает:
создать состояние \(S_x\), последовательно поглотить перечисленные пары
«домен, значение», затем извлечь \(n\) байтов.

### 6.1. Привязка к ключу

\[
Q=\operatorname{Duplex}_{K_{commit},\ell_{commit}}[
(d_H,H),(d_N,N),(d_A,A),(d_F,\texttt{"key-commitment"});16].
\]

Таким образом, \(Q\in\{0,1\}^{128}\). Он позволяет быстро отклонить неверную
комбинацию ключа, nonce и context, но не заменяет 256-битный tag.

### 6.2. Синтетический tag

\[
T=\operatorname{Duplex}_{K_{mac},\ell_{mac}}[
(d_H,H),(d_Q,Q),(d_N,N),(d_A,A),
(d_L,\operatorname{LE}_{64}(m)),(d_M,I),
(d_F,\texttt{"authentication-tag"});32].
\]

То есть \(T\in\{0,1\}^{256}\) зависит от всего внутреннего сообщения, включая
случайный padding.

### 6.3. XOF и ciphertext

\[
Z=\operatorname{Duplex}_{K_{stream},\ell_{stream}}[
(d_H,H),(d_Q,Q),(d_N,N),(d_T,T),(d_A,A),
(d_L,\operatorname{LE}_{64}(m)),
(d_F,\texttt{"encryption-stream"});m],
\]

\[
C=I\oplus Z.
\]

Поскольку \(T\) входит в transcript XOF, разные внутренние сообщения при одном
и том же nonce должны получать разные потоки при идеальном поведении примитивов.
Но если повторятся все \((K,N,A,I)\), то повторятся \(Q,T,Z,C\); это не строгая
гарантия nonce-misuse resistance в принятом криптографическом смысле.

## 7. Контейнер и текстовое кодирование

Бинарный контейнер:

\[
B=H\|Q\|N\|T\|C,
\]

его длина равна

\[
|B|=8+16+24+32+m=80+m.
\]

Текстовая форма:

\[
\texttt{"OV3-"}\|\operatorname{Encode64}(B).
\]

Алфавит для значений \(0,\ldots,63\):

```text
ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz123456789-_~
```

Каждые 24 входных бита делятся на четыре шестибитных числа. Символ `=` не
используется. Для остатка в 8 бит выдаются два символа, для остатка в 16 бит —
три; неиспользуемые младшие биты последнего символа обязаны быть нулевыми.
Поэтому кодирование канонично. Длина всей текстовой строки равна

\[
4+\left\lceil\frac{4(80+m)}3\right\rceil.
\]

## 8. Расшифрование как система проверок

После канонического декодирования и структурных проверок вычисляется

\[
Q'=\operatorname{Commit}_{K}(H,N,A)
\]

и сравнивается с \(Q\) по всем 16 байтам. Независимо от результата commitment
строится кандидат

\[
I'=C\oplus\operatorname{XOF}_{K}(H,Q,N,T,A,m),
\]

а затем

\[
T'=\operatorname{Tag}_{K}(H,Q,N,A,m,I').
\]

Plaintext допускается к разбору только если одновременно

\[
(Q'=Q)\land(T'=T).
\]

Сравнение каждого поля накапливает OR всех побайтовых различий и не завершает
цикл на первом различии. После успешной аутентификации дополнительно проверяется

\[
I'_0=3,\quad I'_1=0,\quad I'_{2..3}=0,
\]

считывается \(p=\operatorname{LE}_{32}^{-1}(I'_{4..7})\), повторно вычисляется
ожидаемое \(m\), и возвращаются байты \(I'_{8..8+p}\). Replay-вариант API после
успешной проверки регистрирует токен \(N\|T\); stateless-вариант свежесть не
гарантирует.

## 9. Число вызовов перестановки

Для поля длиной \(x\) обозначим

\[
b_x=\left\lceil\frac{17+x}{64}\right\rceil,
\qquad u_m=\left\lceil\frac m{64}\right\rceil.
\]

Вывод одного подключа требует 5 вызовов \(P\): одно для algorithm label, два
для 64-байтового ключа, одно для purpose label и одно для final label. Создание
keyed transcript после этого требует ещё 4 вызова.

Если \(a=|A|\), \(m=|I|\), то:

\[
\begin{array}{c|c}
\text{операция}&\text{число вызовов }P\\\hline
Q&12+b_a\\
T&14+b_a+b_m\\
Z&14+b_a+u_m
\end{array}
\]

В строке \(Z\) учтены \(u_m-1\) дополнительных перестановок squeeze. Полное
шифрование или полная криптографическая часть расшифрования требует

\[
40+3b_a+b_m+u_m
\]

вызовов \(P\). Поэтому время и память для сообщения имеют асимптотику
\(O(m+a)\); ciphertext, inner и поток требуют линейной памяти \(O(m)\).

## 10. Что следует строго, а что является предположением

### Строгие свойства реализации

1. P1024-V3 является перестановкой, потому что это композиция обратимых
   Feistel-раундов.
2. Все четыре явно заданных diffusion-слоя имеют бинарный ранг 512 согласно
   точному гауссову исключению над их матрицами.
3. Обрамление полей включает домен, 64-битную длину и два padding-маркера;
   разбиение transcript на поля однозначно в пределах допустимых длин.
4. XOR-поток симметричен: \((I\oplus Z)\oplus Z=I\).
5. Случайный внутренний padding всегда содержит от 1 до 32 байтов.
6. Header, context, nonce, commitment, длина и полное inner входят в
   аутентифицируемый transcript в явно указанных доменах.
7. До успешной проверки commitment и tag содержимое inner не возвращается.

### Идеализированные ориентиры, не доказательства

Если моделировать \(P\) как идеальную 1024-битную перестановку, то 512-битная
capacity обычно задаёт generic birthday-масштаб порядка \(2^{256}\) запросов,
а преимущество многих sponge-атак содержит член порядка \(q^2/2^{512}\).
256-битный tag даёт идеализированную вероятность случайной подделки около
\(2^{-256}\) на одну попытку, а 128-битный commitment — вероятность случайного
совпадения около \(2^{-128}\). Для \(q\) равномерных 192-битных nonce вероятность
хотя бы одной коллизии ограничивается

\[
\Pr[\text{collision}]\le\frac{q(q-1)}{2^{193}}.
\]

Эти числа — цели конструкции в идеальной модели. Они неприменимы как готовое
доказательство к собственной P1024-V3. Не установлены, в частности:

- PRP/PRF-стойкость P1024-V3 и keyed duplex;
- дифференциальные, линейные, интегральные, rotational и алгебраические bounds;
- indifferentiability sponge;
- collision/preimage security, unforgeability MAC и pseudorandomness XOF;
- related-key и multi-user bounds;
- формальная IND-CPA/IND-CCA или nonce-misuse security полной композиции.

Avalanche-тест измеряет лишь выборочные производные

\[
\operatorname{wt}(P(x)\oplus P(x\oplus e_i))
\]

и полезен для обнаружения грубых дефектов диффузии. Значение около 512 бит не
доказывает ни одного из перечисленных криптографических свойств.

## 11. Краткая функциональная запись

Вся схема сводится к формулам

\[
\begin{aligned}
K_{mac},K_{stream},K_{commit}&\leftarrow\operatorname{KDF}(K),\\
N&\xleftarrow{\$}\{0,1\}^{192},\\
I&=\operatorname{Pack}(M;R),\\
Q&=\operatorname{Trunc}_{128}\operatorname{Commit}_{K_{commit}}(H,N,A),\\
T&=\operatorname{Trunc}_{256}\operatorname{MAC}_{K_{mac}}(H,Q,N,A,|I|,I),\\
Z&=\operatorname{XOF}_{K_{stream}}(H,Q,N,T,A,|I|),\\
C&=I\oplus Z,\\
B&=H\|Q\|N\|T\|C.
\end{aligned}
\]

Здесь KDF, Commit, MAC и XOF — не внешние стандартные примитивы, а разные
domain-separated transcripts одного duplex над собственной P1024-V3.
